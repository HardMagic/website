# Controlled BriefLock canary — 2026-09-01

This is redacted operational evidence. The request used synthetic organization,
role, and context values and a test plus-alias mailbox owned by the release
operator. No customer data, raw request body, token, signed URL, or email
address is retained here.

- Source commit: `a39c454`
- Protected pipeline: `56132`
- BriefLock deployment job: `1018319`
- Azure deployment: `hm-brief-lock-51`
- Function zip deployment: `a0703c32-9c61-4536-8743-106b47a0b721`
- Case ID: `brief-lock-schema-and-routing-20260901`
- UTC window: `2026-09-01T02:18Z`–`2026-09-01T02:31Z`
- Route: `POST https://briefs.hardmagic.com/api/brief-request`
- Correlation: request correlation was the fixed synthetic v4 request ID; it is
  intentionally omitted from this durable record.

Expected and observed:

- Front Door response: `303` to the allowlisted Generative Media thanks route —
  observed `303` with the expected `Location`.
- ACS delivery: one field-guide message observed in the operator mailbox.
- Dataverse: one engagement projected with the live `hm_*` columns, the
  Account-bound Contact, HardMagic owner team, and HardMagic BU; `delivery`
  read back as `sent`.
- Retry behavior: the first CRM attempt timed out and the next attempt exposed
  a team-role `403`; after the owner team received the existing Local-depth
  Brief Delivery role, the bounded queue retry succeeded.

Cleanup:

- Exact synthetic engagement and its only Account-bound Contact deleted after
  read-back confirmed no other engagement relationship.
- Exact private ledger object `requests/<synthetic-id>.json` deleted after
  granting and removing a temporary ledger-container Blob role for the
  operator.
- The test message was moved to Deleted Items (not permanently deleted).
- Final D365 engagement/contact counts for the synthetic case: `0` / `0`.
- `TURNSTILE_REQUIRED` restored and read back as `true`.

Owner: HardMagic release operator. No unresolved canary exception remains.
